datasource ekart_users_db {
    provider = "postgresql"
    url      = env("USER_DATABASE")
}

generator client {
    provider = "prisma-client"
    output = "../generated/prisma"
}

model User {
    id        Int    @id @default(autoincrement())
    firstName String
    lastName  String
    email     String
}
